import { Location } from './Location';

export class Service {

  constructor(
    name: string,
    description: string,
    unitValue: number,
    destination: Location,
    startDate: Date,
    endDate: Date,
    createdBy: string
  ) {
  }
}
