export class TransportTypeResponse {
    transportTypeId?: number;  // Identificador único del tipo de transporte, opcional.
    name?: string;  // Nombre del tipo de transporte, opcional.

    constructor() {
        // Constructor vacío para inicialización sin valores.
    }
}
